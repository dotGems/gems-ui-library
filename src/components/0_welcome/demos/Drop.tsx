import React from 'react';
import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({});

/**
 * Demo page featuring Drop UI.
 */
export function Drop() {

    const classes = useStyles();

    return (
        <div>
            Not Implemented
        </div>
    )
}
