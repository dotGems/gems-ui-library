import React from 'react';
import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({});

/**
 * Demo page featuring Chain UI.
 */
export function Chain() {

    const classes = useStyles();

    return (
        <div>
            Not implemented
        </div>
    )
}
